# 决策日志

格式：`[时间] Phase N - 决策描述 - 原因 - 影响`

---

## 2026-08-23

```
[19:18] Phase 0 - 判定为标准路径（非快速路径） - 涉及既有后端性能改造 + 新建前端交付物 + 需压测与回归双重验证，超出轻量级判定条件 - 影响：启用架构师/设计师/后端/前端/QA 五角色，PM 不参与（需求已由用户直接明确，无需竞品调研）

[19:18] Phase 0 - 性能优化目标锁定为「高 QPS 吞吐」+「高并发 SSE 长连接」双目标 - 用户明确选择第 1 与第 3 项，未选低延迟 P99 专项 - 影响：优化重点放在减少 Redis 往返、热点缓存、GC 优化、缓冲区池化；不为单纯压低 P99 做激进改造

[19:18] Phase 0 - 前端形态锁定单文件原生 HTML+JS 内嵌 Go 二进制 - 用户选择，且契合基础设施组件定位（零构建零依赖，内网离线可用，运维零负担） - 影响：禁用所有 npm 图标库与图表库，图标改内联 SVG sprite，图表需纯 SVG/Canvas 手绘；前端无构建链，CI 无需新增 Node 步骤

[19:18] Phase 0 - 鉴权复用现有 Bearer Token + sessionStorage - 用户选择，零后端改动 - 影响：不新增用户表/会话管理/CSRF 防护；不落 localStorage 避免 XSS 持久化窃取；关闭标签页即失效

[19:18] Phase 0 - PM 角色不参与本轮 - 需求边界由用户直接锁定（4 个模块明确列出），无竞品调研必要 - 影响：跳过 PRD 环节，Spec 直接基于用户确认的需求 + 架构师/设计师调研结论生成

[19:18] Phase 1 - 明确禁止推翻两阶段原子求值设计 - 该设计同时解决配额放大(P0-1)与计数器污染(P0-5)，实测 500 并发恰好通过 50 个零误差 - 影响：架构师优化方案必须在此设计之上做增量，不得重构限流内核

[19:18] Phase 1 - 明确禁止以正确性换性能 - 限流判定放宽即为功能缺陷；本地缓存只允许"更严格"方向（既有设计原则） - 影响：任何引入判定宽松化的优化直接否决，即使性能收益显著

[19:50] Phase 1 - 设计门禁通过（设计师产出采信） - 我独立复算 WCAG 对比度，结果与设计师报告逐位吻合（--c-border 1.38/1.31、--c-border-field 3.11/3.63），证明为实测非编造；emoji 与弹跳缓动零命中；50 个 SVG symbol 全部含绘制数据无空壳；#6366f1 仅出现在"刻意避开"注释中未被使用 - 影响：design-tokens 与 icon-sprite 可直接作为前端唯一依据

[19:50] Phase 1 - 采纳降级态独立青色 #0d6b78，不复用琥珀警告色 - "Redis 故障本地兜底放行"与"配额接近上限"是性质不同的两件事，混色会让人在故障中误判处置方向 - 影响：五态语义色扩展为 ok/warn/danger/degraded/off 五种，且五态各配不同形状（圆/三角/菱/方/空心环），色盲用户不依赖颜色亦可区分

[19:50] Phase 1 - 交叉验证设计规格与后端真实 API 契约一致 - 逐项核对：202 saved_but_publish_failed 分支(server.go:368)、audit LIMIT 100(server.go:457)、detail 4000 字符截断(server.go:495) 全部命中 - 影响：前端可直接按 DESIGN.md 实现，无需再次核对后端；设计师确实读了代码而非凭想象写规格

[21:40] Phase 3 - 【后端交付通过门禁，变异测试独立验证】安全缺陷 + Tier1 热更新 + 部署文档三项完成 - 我逐项独立验证而非采信自述：①占位令牌 change-me-... 从 200 变 401，真实令牌 200，无凭证 401 ②无 .env 时 docker compose config 退出码 15 ③e2e 50/50 且 panic 计数 0 ④race 全量通过 - 影响：管理面鉴权缺陷已闭环；总覆盖率 47%→52.1%，limiter 72.7%→80.4%，admin 19.9%→44.8%，config 0%→40.2%；limiter 测试数 13→25 且零跳过

[21:40] Phase 3 - 【变异测试证实 panic 回归测试真实有效】我移除 key.go 的 winSec<=0 防御后测试立即复现 integer divide by zero - 断言覆盖 winSec=0/-1/-3600 三种情形与 natural 双分支，不是摆设测试 - 影响：可信任该测试守护此缺陷不复发

[21:40] Phase 3 - 【贝洛奇发现并修复清单外的严重既存缺陷】Redis 故障期间每请求 panic（integer divide by zero）- 并发规则不解析 Window 致 winSec 恒为 0，而 failModeOf 将其归入 FailLocalQuota，降级路径无条件调 WindowBoundary 触发除零。客户端看到连接重置而非 5xx。之所以长期未暴露：仅在熔断器真正打开后可达，熔断未开走 Fail-Close 分支不进 degrade()，演示/单测/Redis 短暂抖动均碰不到 - 影响：防御放在做除法那一行而非分发给 5 个调用点自查（正确判断：靠"每处都记得检查"等于把必然会漏的义务分发出去）；这是本轮价值最高的发现，超出原任务范围

[21:40] Phase 3 - 【我亲自修复偶发测试】TestTokenBucketPersistentAcrossWindow 约 1/5 概率失败 - 原断言写死区间 [3,8] 且 sleep 520ms 期望"约 5 个"，宿主负载高时 sleep overshoot 到 ~1s 会恢复满 10 个而超界。依赖 wall-clock 精度的固定区间断言本身不可靠 - 影响：改为按实测 elapsed 推算自适应上界（theoretical = elapsed×rate，容差 +2，封顶 burst）。实测 5 次全过且上界随耗时自适应（520ms→上界7，若 overshoot 到 1s 则自动升到 10）。关键是没有把测试改宽松：仍能捕获"整桶重置"这一 P0-2 回归，因为分界不是"恰好5个"而是"恢复量是否与等待时间成正比"

[21:15] Phase 1 - handler.go 编译阻塞解除，基线关键路径打通 - 贝洛奇已修好 atomic.Pointer[Options] 改造；我实测 gofmt 干净 / build 通过 / vet 通过 / 七个包测试全 ok - 影响：架构师可执行完整基线采集。已要求其在基线文件头部记录代码版本标识（基线采集于 atomic.Pointer 改造后的版本，若无标识后续对比会不知对应哪版代码）

[21:15] Phase 1 - 【发现资源争抢风险，需时序协调】两个 agent 都要重启 compose - 贝洛奇后续要改 compose 凭证必填并重启验证，架构师压测期间被重启会直接毁掉数据；且宿主仅 5 核，压测与 go test -race 同时跑会互相污染 - 影响：要求架构师跑基线前先报告，由我协调独占时段；这是共享 5 核环境下的真实约束，不是流程形式

[21:15] Phase 1 - 【发现 worker 交付遗漏】架构师首次标记完成时三项交付均未落地 - test/perf 目录不存在、基线未采集、ADR-008 仍写 k6（第25/36/178行）。根因：worker 唤醒后只读最新消息未重读完整任务清单，只完成了数据修正部分 - 影响：重新派发时把指令收窄到单一目标并给出逐项确认清单；教训是多阶段任务在 worker 被唤醒后需重申完整清单，不能假设其记得原始指令

[21:00] Phase 1 - 【核心数据修正已独立证实】架构师撤回 +98%，修正为 +28~30% - 我从 Redis 重置后的累积统计取到 evalsha 22.18µs/call（15.3 万次样本），与其修正值「真实 batch_check.lua 22.3µs」精确吻合，独立证实修正正确。此前我看到的 3.56µs 确实被其简化探针（3.70µs）稀释——根因不是采样时段而是脚本不同，用简化脚本推导往返收益会系统性高估 - 影响：真实容量基线更正为单往返 ~31,000 QPS / 双往返 ~22,400 QPS（非 88k/45.5k）；POC 阈值从「<50% 回退」重定为「≥20% 达标，<10% 回退」

[21:00] Phase 1 - 归因结论：88k 平台期非 Redis 饱和，但瓶颈确实在 Redis - 双侧 CPU 同步采集显示 Redis 全档跑满单核（0.998-0.999 核），客户端仅 0.64-0.70 核未饱和；QPS 从 32.6k 到 31.3k 不增长而 p50 涨 17.6 倍，为教科书式饱和曲线。交叉校验：30,577 × 21.93µs = 0.671 核，与 INFO cpu 的 0.998 核吻合（差额为网络 I/O 与连接管理），两个独立数据源互证 - 影响：我的质疑方向正确但结论相反；ADR-008 已把双侧 CPU 采集升为强制项，因为本次失准根因正是「只看 QPS 平台期不看 CPU 归属」

[21:00] Phase 1 - 冷热差表述改为数量级而非具体倍数 - 我提的 40 倍与架构师原先的 9 倍均不成立：144.67µs(eval) 与 3.56µs(evalsha) 样本来自不同脚本，eval 那 6 次是三个正式脚本首次加载，evalsha 累积绝大部分是简化探针，分母口径不同 - 影响：改为「冷路径百微秒级 vs 热路径个位数到二十几微秒级，差一个数量级以上」，足以支撑必须预热且不引入新的错误精度

[21:00] Phase 1 - 【发现跨 agent 文件冲突】internal/proxy/handler.go 编译失败 - handler.go:74 声明 opt atomic.Pointer[Options] 但 import 缺 sync/atomic；handler.go:193 已按新签名调用 h.reject 传 opt 但函数定义仍是旧签名。系贝洛奇 Tier1 热更新改造的半成品状态，与架构师「源码零改动」声明冲突（架构师只在 test/perf/ 工作，未碰 internal/）- 影响：已告知贝洛奇修复，我不插手以免造成三方冲突；同时提醒其在 ServeHTTP 入口取一次 opt 快照全程复用，否则热更新发生在请求中途会导致同一请求前后半段用不同配置，正是其注释里要避免的混合状态

[20:45] Phase 1 - 压测工具从 k6 改为自写 Go 程序 - 实测拉取 grafana/k6:latest 失败（网络持续重试超时），wrk/vegeta/hey 镜像同样不可得，ADR-008 原选型落不了地。已验证替代路径可行：golang:1.22-alpine 本地已有，容器内经服务名 gateway:9091 可直达（返回 {"status":"alive"}）- 影响：改为 test/perf/ 下自写 Go 压测程序。此选择并非退而求其次，反而更优：①零外部镜像依赖，离线内网同样可跑 ②能精确实现 ADR-008 要求的 30s 强制预热、5 轮中位数、场景B零误差断言 ③可同步采集 Redis CPU% 直接解决 EVALSHA 归因问题 ④不引入新技术栈。硬约束：必须与主工程隔离，go build/go test ./... 不受影响

[20:45] Phase 1 - 确立「基线先于优化」的执行顺序 - 当前实例已运行 5 小时并经历 e2e 与压测探针，指标已污染，无法作为对照点 - 影响：要求架构师本轮只做压测基础设施 + 基线采集 + ADR 修正，明确禁止改动 internal/ 源码做优化；优化实施为后续独立环节

[20:35] Phase 1 - 【门禁复核发现数据归因问题，已打回澄清】架构师报 EVALSHA 18.78µs/call，我独立从 Redis 累积统计取到 3.56µs/call（105 万次调用样本），相差 5.3 倍 - 该数字是其核心推导链的基础：「Redis 88k EVALSHA/s 饱和 → 端到端上限 = 88k÷往返数 → 减少往返是最高收益项」。若单次成本真为 3.56µs，Redis 单核理论上限约 28 万/s，则 88k 平台期很可能不是 Redis 饱和，而是压测客户端与 Redis 争抢同一批 5 核所致（空载实测 Redis CPU 仅 0.12%）- 影响：#1 合并往返方向仍成立（减少一次网络往返在任何口径下都是净收益），但 +98% 的收益量级归因需重做；POC 验收阈值必须建立在正确基线上，否则达标判断标准本身是错的。已要求压测时同步采集 Redis CPU% 以区分瓶颈归属

[20:35] Phase 1 - 冷热差实测 40 倍而非架构师所述 9 倍（eval 144.67µs vs evalsha 3.56µs）- 这一偏差方向对其结论有利，强化了「压测必须 30s 预热否则数据作废」的论证 - 影响：ADR-008 数字需更正为实测值，理由更硬

[20:35] Phase 1 - 独立验证架构师「源码零改动」声明为真 - gofmt 干净 / vet 通过 / race 全绿 / 限流内核 13/13 零跳过 / 探针文件已清理（列出的 _test.go 均为既有文件）- 影响：可安全进入实施阶段，无需回滚其调研期改动

[20:20] Phase 1 - 【实测发现真实安全缺陷】compose 给 ADMIN_TOKEN 配了仓库可见的默认值 change-me-admin-token-32chars-min，实测用它访问 /admin/snapshot 返回 HTTP 200 - 该令牌公开写在仓库中，等于管理面无鉴权。ErrNoToken 只防"忘记设置"，没防住"用了公开占位值"，而占位值更危险因为看起来像已配好；33 字符轻松通过 >=16 校验，长度检查在此完全无效。同类问题涉及 MYSQL_PASSWORD/MYSQL_ROOT_PASSWORD/REDIS_PASSWORD/GRAFANA_PASSWORD 共 5 项 - 影响：compose 改用 ${VAR:?} 必填校验；令牌阈值提到 32 字符 + 弱值黑名单；新增 make init 生成强随机凭证。降低部署门槛的正确做法是一键生成凭证，不是给默认值

[20:20] Phase 1 - 【用户提出】配置分层落地为三层，产出 CONFIG-TIERING.md - 用户要求"能默认就默认"+"部分配置放管理页面"。判断标准定为「能否安全热更新」+「误配爆炸半径」，而非"方便不方便" - 影响：Tier0-A 五项凭证移除默认值；Tier0-B 引导参数保留默认；Tier1 七项运行策略搬进页面（EXPOSE_RULE_NAME/UPSTREAM_TIMEOUT/TOKEN_FLUSH_INTERVAL/MAX_REQUEST_BODY_MB/CONFIG_POLL_INTERVAL/LOG_LEVEL/INSTANCES）

[20:20] Phase 1 - 明确拒绝把安全类配置搬进管理页面 - ADMIN_ALLOW_CIDRS 若可页面改，攻击者拿到令牌后能自行放开来源限制、自毁第二重防线；ALLOW_HEADER_UPSTREAM 与 UPSTREAM_ALLOWLIST 是 SSRF 防线，交给一个令牌保管风险过高；TRUSTED_PROXY_HOPS/REAL_IP_HEADER/TOKEN_HEADERS 误配会导致限流维度整体错乱（所有 IP 被识别为同一个）；TZ_OFFSET_SECONDS 运行中改动导致窗口边界错乱 - 影响：这些项即使技术上可热更新也保留在环境变量，需重启才能改

[20:05] Phase 1 - 【用户提出，已采纳】看板指标数据源改为 admin 端口同源受鉴权端点，不从 obs 端口 /metrics 跨源取数 - 用户指出「看板本质就是管理员的一部分」。核实确认：obs(29091) 绑 0.0.0.0 且无鉴权，admin(29090) 绑 127.0.0.1 有 Bearer+CIDR。若看板跨端口取数需在无鉴权端口开 CORS，等于让任意网页可读含 biz 名/规则名/拒绝分布的内部拓扑信息；且同一界面三模块要令牌、监控模块裸奔，鉴权边界不成立 - 影响：后端需在 admin 新增指标端点；前端全部请求同源同鉴权，无需 CORS；obs 端口职责收窄为仅供 Prometheus 抓取。这是我此前遗漏的边界错位，用户先发现

[19:50] Phase 1 - 我主动修正设计产出两处缺陷 - ①icon-sprite.svg 头注释写"共 42 个"实际 50 个，会误导前端 ②--c-border-strong 命名有误导性（1.84:1/1.77:1 仍不可用于控件，名字里 strong 指视觉层级非对比度合规） - 影响：已就地修正注释并补约束说明，避免前端误用装饰线作控件边界

[20:45] Phase 1 - 【架构师实测定标】Redis 在约 88,000 EVALSHA/s 饱和，这是网关吞吐的硬天花板 - 容器内实测：PoolSize 从 8→512 爬坡，QPS 在 PoolSize≥32 后完全持平（87-88k），512 反而回落至 81k 且 p99 恶化；并发从 16→1024 爬坡，QPS 仅增 22% 而 p50 从 209µs 涨到 11.4ms，典型饱和曲线 - 影响：容量规划公式确定为「端到端 QPS 上限 = 88k ÷ 每请求往返数」；连接池标定改为「不成为瓶颈」而非「越大越快」，PoolSize 建议 128（现状 256 偏大但无害）；一切优化的收益上限由此界定

[20:45] Phase 1 - 【最高收益项】减少每请求 Redis 往返数是唯一能突破吞吐天花板的手段 - 实测「2 往返 vs 1 往返」：吞吐 45.5k vs 90.1k（−47%），512 并发 p99 15.2ms vs 8.7ms（翻倍）。现状 handler.go:150 TokenAdmit 与 :156 Check 是两次串行往返，且 TokenAdmit 对每条 token 规则各发一次 - 影响：ADR-003 提出把 token 准入合并进 batch_check.lua 的 Phase 1（Phase 2 为 no-op，不破坏两阶段不变量）；因需触碰核心脚本，标记为「需 POC 验证」并登记 OPEN-DECISIONS，POC 须证明净吞吐提升 >50% 且 500 并发打 limit=50 仍零误差

[20:45] Phase 1 - 明确区分「CPU/GC 优化」与「延迟优化」，避免夸大收益 - 准入路径微优化（手写编码替代 json.Marshal + Key 前缀预计算 + 负缓存原子短路）实测可把 1785ns 降至约 475ns（−73%）、分配 1144B/27allocs 降至 408B/13allocs，但相对 Redis 往返 p50 2.4ms（256并发）占比不足 0.06%，**不会体现在 P99 上** - 影响：这些项的价值是提高单核可承载 QPS 与降低 GC 压力，不是降延迟；ADR-001 明确写入「任何把本项宣传为降低 P99 的说法都是错的」，防止实施方与 QA 用错误口径验收

[20:45] Phase 1 - SSE 读缓冲池化收益显著且不触碰零缓冲红线 - 实测每流 16KB 固定分配（stream.go:51），并行场景池化后 4161ns→1503ns（−64%）、分配 16417B→36B（−99.8%）；1 万并发流可省约 160MB 峰值分配 - 影响：池化对象是**上游读缓冲**而非下游写缓冲，逐行读+每帧 Flush 行为一行不改；强制要求 Put 前 Reset(nil) 否则从内存优化变内存泄漏；验收硬闸门为 TTFB 不得高于基线 5ms，上升即说明引入攒包必须回退

[20:45] Phase 1 - 【实测发现真实隐患】PoolTimeout(1s) > REDIS_TIMEOUT(200ms)，导致「连接池不足」伪装成「Redis 故障」并误触发熔断降级 - go-redis 的 waitTurn 会阻塞至 PoolTimeout，而 Check() 的 ctx 只有 200ms，于是池饱和先表现为 ctx 超时，再被 limiter.go:243 计为 Redis 故障。main.go:275 的注释其实已预见此事但无指标证实 - 影响：PoolTimeout 应设为 REDIS_TIMEOUT 的 50%（100ms）使池不足以 ErrPoolTimeout 快速失败；同时必须暴露 pool_timeouts_total 等 5 项池指标（数据源 UniversalClient.PoolStats() 接口本就提供，拿不到不是技术限制而是没接）

[20:45] Phase 1 - 【安全】go-redis v9.5.3 已被官方撤回且含 CVE-2025-29923，本项目正好命中其触发条件 - 该漏洞在 CLIENT SETINFO 建连超时时导致响应乱序，触发条件之一是「配置了激进超时」，而本项目 Read/WriteTimeout=1s、Check ctx=200ms 正是如此。对限流网关而言一次乱序响应=一次错误限流判定，属典型沉默逻辑错误 - 影响：立即设 DisableIndentity=true（官方给出的 workaround，注意官方拼写错误）；同时建议升级至 v9.6.3+ 根治并脱离 retracted 版本

[21:10] Phase 1 - 【自我纠正，team-lead 复核发现】撤回「合并往返 +98%」估算，修正为 +28.2%~+30.4% - team-lead 指出 EVALSHA 单次成本口径矛盾（我报 18.78µs vs 累积 3.56µs）。查明根因：我首版用的是**简化探针脚本**（3.70µs/call），而真实 batch_check.lua 是 **22.3µs/call，贵 6 倍**；用简化脚本推导往返收益系统性高估。用真实 batch_check.lua + token_ledger.lua 重测（CONFIG RESETSTAT + 400次预热 + 5s采样）：256并发 21,848→28,004 QPS(+28.2%)，512并发 22,438→29,266 QPS(+30.4%)，p99 −15.9%~−18.8%。不翻倍的原因：ledger admit 只有 GET+PTTL（12.6µs），比 batch_check（22.3µs）便宜得多，两者成本不对等 - 影响：ADR-003 收益全面重写；POC 阈值从「<50% 回退」改为「≥20% 达标、<10% 回退」（依据：真实实测 +28~30%，纯check理论上界 +38%，合并后已取得上界 93~96%，剩余是 GET+PTTL 固有成本）

[21:10] Phase 1 - 【归因已证实】88k 平台期确实不是 Redis 饱和点，但瓶颈**仍在 Redis** - team-lead 质疑 88k 可能是客户端与 Redis 争抢 5 核所致，该质疑方向正确（88k 口径本身就错）。用双侧 CPU 同步采集给出决定性证据：真实脚本下 conc 64→1024 全档 **Redis CPU 恒为 0.998-0.999 核即 100% 跑满单核**，客户端仅 0.64-0.70 核未饱和；QPS 完全不增长（32.6k→31.3k）而 p50 涨 17.6 倍（1.82ms→32.03ms），教科书式饱和曲线。交叉校验：30,577 QPS × 21.93µs = 0.671 核，与 INFO cpu 的 0.998 核吻合（差额为网络I/O与连接管理），两独立数据源互证 - 影响：真实容量基线定为**单往返约 31,000 QPS / 双往返约 22,400 QPS**；容量公式改为 `上限 ≈ 1e6 ÷ 脚本µs/call ÷ 往返数`，并注明随规则数变化、须用目标业务真实规则集实测；ADR-004/006/008 中所有 88k 表述已更正

[21:10] Phase 1 - 【压测方法论硬化】双侧 CPU 采集升为强制项，无此数据的压测结论不予采信 - 这次归因失准的根因就是只看 QPS 平台期而未看 CPU 归属，把「脚本口径不同」误读为「Redis 饱和」。判据明确：Redis满核+客户端未饱和=瓶颈在Redis；Redis未满+客户端满=**数据无效须加客户端资源重测**；两者都未满=瓶颈在 gateway/网络需 pprof - 影响：ADR-008 新增该判据；同时更正冷热差表述——team-lead 提出的「40 倍」（144.67 vs 3.56）同样不可直接相除，因 eval 与 evalsha 累积样本来自**不同脚本**，分母口径不同；改为表述「冷路径百微秒级 vs 热路径个位数到二十几微秒级，差一个数量级以上」，足以支撑必须预热且不引入新的错误精度

[20:45] Phase 1 - 否决 5 项常规但在本项目为伪优化的方案，并给出量化依据 - ①Redis Pipeline 跨请求攒批：既破坏两阶段原子性（引入跨请求中间态，正是 P0-5 反面），且 Redis 已 CPU 饱和，减少 RTT 不减少脚本执行成本 ②Redis Cluster：batch_check 的 KEYS 跨多维度会直接 CROSSSLOT 报错，属架构级改动 ③fasthttp 替换 net/http：瓶颈不在 HTTP 解析，且不支持 HTTP/2、与 SSE Flusher 语义冲突 ④HashToken 换非加密哈希：仅占 7.8%（138.7ns）而碰撞意味着两用户共享配额，属安全问题 ⑤quotas map 原子短路：与 blocks 不同，降级期它是唯一判定依据，短路会放宽限流 - 影响：实施方聚焦 ADR-001/002/003/004/005，不在伪优化上耗时；同一手法（原子短路）在 blocks 安全而在 quotas 危险的区分已写入 ADR-006 留档

[22:05] Phase 1 - 【ADR-003 已实施】合并往返 POC 达标：QPS +32.4%~+35.9%，远超 ≥20% 阈值 - 用真实 batch_check.lua 交替 3 轮取中位数：128/256/512 并发下 23,754→32,287 / 22,839→30,448 / 22,740→30,112，p50 降 22.7%~27.3%，p99 降 16.0%~18.3%。单次脚本成本从 12.4µs 升到 22.3µs（多一次 GET+PTTL + 多解一个规则对象），但往返数减半故净收益为正，与 ADR-004 容量公式 `1e6÷脚本µs÷往返数` 吻合。Redis CPU 全档 0.999 核，证明瓶颈仍在 Redis 单线程、归因成立 - 影响：token 准入并入 batch_check Phase1（t='tkadmit'，只读不扣减，Phase2 为 noop），handler.go 移除独立 TokenAdmit 调用但保留方法（仍有测试覆盖）；ADR-003 转 Accepted & Implemented，OPEN-DECISIONS 该项关闭

[22:05] Phase 1 - 【差点造成 P0-1 复现】发现 ipairs 遇 nil 即停止会使 Phase2 静默跳过后续所有提交 - batch_check.lua 的 Phase2 用 ipairs(plans) 遍历。tkadmit 是「只读不写」规则，若不在 plans 占位就留下空洞，其后所有规则的 INCRBY/ZADD 全被跳过——判定通过但配额没扣，即 P0-1 配额放大复现，且不报错不崩溃、既有 25 个测试全绿也发现不了。典型沉默逻辑错误 - 影响：tkadmit 与未知类型均写 plans[i]={t='noop'} 占位；新增 TestTkAdmitDoesNotBreakLaterRuleCommit 刻意把 token 规则排在 fixed 之前；**已做变异验证**——手动移除占位后该测试确实变红（放行 10 个而非 3 个），证明防线有效而非摆设

[22:05] Phase 1 - 保留 TokenAdmit 方法而非删除，并新增两条路径等价性测试 - 该方法仍被 limiter_test.go 覆盖，删掉会减少一条独立的语义参照。新增 TestTkAdmitSemanticsMatchLedgerAdmit 断言 used==limit-1 放行、used==limit 拒绝两条路径逐位一致（都用 used >= limit 而非 used+cost > limit，因准入期不知本次消耗量） - 影响：合并实现有独立参照可比对，未来若有人改动任一路径，等价性测试会立即发现偏差
```

---

## 项目总监门禁验证记录（team-lead 独立复核，非采信自述）

```
[22:10] Phase 3 门禁 - ADR-003 POC 通过，我独立复跑四项验证全绿 - race 无数据竞争；limiter 29 项 PASS / 0 SKIP / 0 FAIL；e2e 50/50；场景 B 三轮全部「通过=50 拒绝=450」零误差且 breaker=0 in_flight=0 - 结论：合并往返后 P0-1/P0-5 语义完好，可采信 +32.4%~+35.9% 的收益结论

[22:10] Phase 3 门禁 - 【我独立做了变异验证】ipairs 空洞防线真实有效 - 手动删除 batch_check.lua 中 tkadmit 分支的 plans[i]={t='noop'} 占位后，TestTkAdmitDoesNotBreakLaterRuleCommit 立即变红并报「放行 10 个，期望恰好 3 个」，精确复现 P0-1 配额放大 - 结论：这不是摆设测试。该陷阱最危险处在于不报错不崩溃，原有 25 个测试全绿也发现不了，属静默逻辑错误

[22:10] Phase 3 门禁 - 压测程序两处「失败」实为设计意图的正确体现 - ①未传 REDIS_PASSWORD 时报「健康检查失败，拒绝产出垃圾数据」并退出 ②未传 -cpufile 时判 FAIL 并说明「无此数据结论不予采信」 - 结论：均非缺陷。程序拒绝在依赖不可达或归因数据缺失时给出结论，正是 ADR-008 将双侧 CPU 采集升为强制项的落地

[22:10] Phase 3 门禁 - 修正未决登记册一处遗留错误表述 - 第 28 行仍写「EVALSHA 冷热差异达 9 倍」，但该数字与我提的「40 倍」均不成立（样本来自不同脚本，eval 计数是三个正式脚本首次加载，evalsha 累积值绝大部分是简化探针，分母口径不同不可直接相除）- 已改为「差一个数量级以上」并在括号内保留该数字两次写错的原因说明，供后续引用者参考

[22:10] Phase 3 门禁 - 登记技术债：6 个既有生产文件超「单文件 ≤300 行」规范 - handler.go(561)/server.go(528)/limiter.go(458)/store.go(365)/metrics.go(327)/main.go(324)。判定本轮不拆，理由：①这些正是多人同时改动的核心文件，此刻重构必然冲突 ②拆分不改变行为，无法用测试证明「拆对了」，只能靠 review 而 review 成本此刻最高 ③测试文件不适用同一标准，用例线性增长属正常 - 影响：一刀切要求全拆会制造真实风险，换来的只是数字合规
```

```
[23:35] Phase 3 门禁 - gzip 通过，且前端纠正了我的测量方法错误 - 我用 `cat * | gzip` 得 58,825 字节，那是单流压缩、跨文件复用字典，HTTP 场景不成立；逐文件独立压缩实测 72,458 字节才是真实传输量。运行时首屏 24 个资源 gzip 后总量 70.1KB - 影响：达标判定仍 PASS（< 120KB），但验收基线改用逐文件口径。若沿用我的乐观数字，某天资产增长时会误判仍达标

[23:35] Phase 3 门禁 - gzip 实现的四处细节判断正确 - ①启动时预压缩而非提交 .gz 进仓库：后者需引入生成步骤（违背零构建链），且改了 js 忘重新生成会出现"压缩版是旧代码"这类极难排查的问题 ②`Vary: Accept-Encoding` 在压缩与非压缩两条路径**无条件**设置，只在压缩分支设会留缓存投毒洞 ③压缩分支显式 `Accept-Ranges: none`：ServeContent 按传入内容算 Range，对压缩字节做 Range 会返回错误片段 ④按 RFC 9110 解析 q 值而非 `strings.Contains(h,"gzip")`：`gzip;q=0` 是明确拒绝，子串匹配会当成接受并发出客户端读不了的字节 - 影响：我实测 q=0 确实被正确拒绝，解压后与原文逐字节一致

[23:35] Phase 3 门禁 - 我自己犯了一处测试路径错误 - 用 /assets/app.js 取资源，实际路由是根级 /app.js，服务器正确地把未知路径兜底成 SPA 首页，导致"解压不一致"的假告警 - 影响：换正确路径后三个文件全部字节一致。教训：验证失败时先确认自己的调用方式，别急着判定被测对象有缺陷

[23:35] Phase 3 门禁 - 【QA 发现两条文档缺陷，我已修】DEPLOYMENT.md 指标名写错 + compose --format 不可执行 - ①文档写 unirate_degraded_total，真实名 unirate_degraded_decisions_total（registry.go:53），照文档排障会 grep 到空输出从而误判"没发生降级"，故障处置中的误导性信息比没有信息更糟 ②`docker compose ps --format '{{.Status}}'` 在本机报 parsing failed，已改用 docker inspect（各版本行为一致）- 影响：顺手做了全文指标名交叉扫描（文档提到的 unirate_* 与运行时实际暴露做 diff），零其他不一致

[23:35] Phase 3 - 【我造成一次环境中断，责任在我】QA 终验期间全栈被停 - 我跑 docker compose up -d --build 验证前端改动，重建会先停旧容器，导致 QA 的限流精度与压测两项结果作废。QA 取证判断准确（Exited(0) 优雅退出说明是外部生命周期命令，且 loadgen 无 teardown 逻辑）- 影响：已拉起环境并承诺终验期间不再执行 compose 操作，让 QA 独占。教训：共享环境下重建镜像属于破坏性操作，需先确认无人在跑依赖该环境的验证
```

```
[23:45] Phase 4 - 【QA 终验结论：PASS，Silver 档】P0=0 / P1=0 / P2=3 - 测试维度评为 Gold（160 测试 0 失败 0 跳过、变异测试 5/5 变红、限流精度 6 轮独立测量全部恰好 50、hotreload 连跑 25 次零失败）。契约/安全/无障碍/性能四维为 Silver，总档取最低档 - 影响：可交付。性能维扣分理由诚实——「两侧均未饱和，瓶颈可能在客户端，容量结论尚未成立」，需专用压测环境才能给权威数字

[23:45] Phase 4 - 【P2-3 已闭环，我亲自修 + 变异验证】cmd/gateway 零覆盖导致装配链路无守护 - QA 指出 /admin/metrics 的 Metrics 注入在 main.go:204，若该行被误删则端点从 401 变 503，而 internal/admin 全部单测仍会绿（测试用 newTestServer 自行装配 Options），只有 e2e 或人工能发现。本轮该端点确实出现过「代码写好但镜像未重建 → 404」的状态，链路脆弱性是实证的 - 影响：e2e G 段新增 5 条断言（无凭证 401 / 带凭证 200 / 两端口逐行一致 / no-store / nosniff），e2e 从 50 升到 55。**变异验证：删掉 main.go:204 注入后 5 条全部变红并报出精确病因「main.go 的 admin.Options{Metrics: ...} 可能被删除」**，恢复后 55/55 全绿

[23:45] Phase 4 - P2-1 与 P2-2 在 QA 出报告前已被我修掉 - 指标名 degraded_total → degraded_decisions_total、compose --format 改 docker inspect。QA 报告中这两条的状态需以修复后为准 - 影响：实际遗留 P2 仅 P2-3，且已闭环。三条 P2 全部关闭

[23:45] Phase 4 - QA 独立复现了 Redis 故障期降级行为的完整正确性 - 停 Redis → 120 并发（含 rate+concurrency+token 三类规则）→ panic=0；并发规则本地保守配额恰好放行 20（max_concurrent=20）；/ready 仍 200 不误摘流；/health 正确报 degraded - 影响：这不只验证了「不 panic」，还验证了降级后配额计算的精确性，比我要求的更深一层
```

```
[00:05] Phase 4 - 【本轮交付完成，我独立终验全绿】gofmt 干净 / vet 通过 / race 9 包 exit=0 / limiter 29 PASS 0 SKIP 0 FAIL / e2e 55/55 exit=0 / 限流精度 3 轮全部「通过=50 拒绝=450」零误差 - 影响：可交付。压测程序报「结论：FAIL」是因未传 -cpufile，程序拒绝在缺双侧 CPU 数据时给归因结论，为 ADR-008 强制项的正确体现

[00:05] Phase 4 - 前端负值丢弃三项全过，第三项（重置后能重建基线）是我特别要求补的 - 实测：重启前 9.99 req/s → 重启瞬间 KPI 数组变空无负尖刺 + 提示「网关已重启，指标基线已重置」→ 后续流 10.0 req/s 基线重建成功且提示自动消失。前端不只看 KPI 数字，还取 SVG path 的 d 属性提取所有数值筛负数，确认几何层面零越界坐标 - 影响：只丢弃负值不重建基线会让重启后永久显示「—」，被读成「没流量」而非「出错了」，比负尖刺更隐蔽。该行为现有契约测试守护（store.restarted=true 与 store.kpi=null 必须同时存在）

[00:05] Phase 4 - 前端主动补 7 项契约测试，其中一条价值最高 - 「速率必须 /dt 且 dt 取两帧真实时间戳而非轮询常量」：漏掉除法的话 5s 间隔下 QPS 恰好偏大 5 倍，量级看着合理没人会发现。这类缺陷不崩溃不报错、数量级落在合理区间，只是所有数字都是谎话，靠 review 抓基本靠运气 - 影响：该约束同时防住两个变体（漏除法、用配置间隔代替实际经过时间，后者在 document.hidden 暂停后恢复时会算错）

[00:05] Phase 4 - P99 显示改为带桶区间标注（前端主动发现并修正）- 先独立验算插值确认公式无误，再量化桶宽发现真问题：每桶区间宽度占其上界 50%-100%，(1s,2.5s] 那档宽 1500ms，而原显示 2479.0 ms 暗示 0.1ms 精度，差四个数量级。SRE 会拿它对 SLO - 影响：改为数值下方常显「桶区间 0–1ms」，有效数字按桶宽给，落 +Inf 桶显示 >30000。刻意不放 tooltip：精度限制是数值本身的一部分，藏起来等于没人看。一个假装精确的数字比标注了误差的粗糙数字危险得多

[00:05] Phase 4 - 前端处理自身工具缺陷的顺序正确 - scan_test.go 的 stripJSCode 连字符串内容一起剥，而待断言的端点路径本身就在字符串里，三条断言必然误报。它没有因为「测试失败了」就改产品代码，而是先确认浏览器实测正常、再定位到自己工具有问题，拆出 stripJSComments 并写清两者分工 - 影响：反过来做就是为了让测试变绿而破坏正确代码
```
